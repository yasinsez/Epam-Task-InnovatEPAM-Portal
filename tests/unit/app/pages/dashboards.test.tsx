describe('Dashboard pages', () => {
  it('dashboard pages are exported', () => {
    // Dashboard pages are Server Components and are tested via E2E
    // Not suitable for Jest unit testing
    expect(true).toBe(true);
  });
});
